<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core">
    <span>${%Automatically discard results older than}</span>
    <input type="number" id="${strategy.id}_days" name="discardResultsOlderThanDays" value="${strategy.days}" size="3"
           class="jenkins-input gbs-days" data-id="${strategy.id}_checkbox"/>
    <span>${%days}</span>
</j:jelly>
